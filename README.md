# Доля рынка — Аналитика недвижимости

Веб-приложение для визуализации долей рынка застройщиков по городам.

## Функционал

- 🔐 Авторизация по паролю (пароль: `5555`)
- 🏙️ Переключение между городами: Казань, КА, Пермь, Тольятти
- 📊 Три метрики: Доля в шт., Доля в м², Доля в руб.
- 📅 Фильтрация по месяцам
- 📈 График динамики по всем месяцам
- 📋 Таблица с данными

## Установка и запуск

### 1. Убедитесь, что установлен Node.js
Скачать: https://nodejs.org/ (версия 18+)

### 2. Установите зависимости
```bash
cd market-share
npm install
```

### 3. Запустите приложение
```bash
npm run dev
```

Приложение откроется в браузере по адресу: http://localhost:3000

## Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `dist/`

## Структура проекта

```
market-share/
├── public/
│   └── favicon.svg
├── src/
│   ├── App.jsx          # Основной компонент
│   ├── main.jsx         # Точка входа
│   └── index.css        # Глобальные стили
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## Добавление данных

Данные находятся в файле `src/App.jsx` в объекте `marketData`. 
Структура данных:

```javascript
{
  'Город': {
    'Месяц': [
      { 
        name: 'Застройщик',
        units: 100,           // Количество квартир
        unitsPercent: 10.5,   // Доля в %
        sqm: 5000.00,         // Площадь в м²
        sqmPercent: 12.3,     // Доля в %
        rub: 500000000,       // Выручка в рублях
        rubPercent: 11.2      // Доля в %
      },
      // ...
    ]
  }
}
```

## Планы развития

- [ ] Загрузка данных из Excel-файла
- [ ] Больше городов
- [ ] Экспорт в PDF/PNG
- [ ] Сравнение периодов

---

Разработано для Унистрой © 2024
