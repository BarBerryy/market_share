# 📊 Доля рынка новостроек

React-приложение для анализа рыночной доли застройщиков с загрузкой данных из Google Sheets через Apps Script API.

**Таблица остаётся приватной** — доступ только через ваш скрипт.

## 🛠 Технологии

- **React 18** — UI библиотека
- **Vite** — сборщик
- **Recharts** — графики
- **Google Apps Script** — API для данных
- **CSS Modules** — стилизация

## 🚀 Установка и запуск

### 1. Установите зависимости

```bash
npm install
```

### 2. Запустите в режиме разработки

```bash
npm run dev
```

Приложение откроется на http://localhost:3000

### 3. Сборка для продакшена

```bash
npm run build
```

Готовые файлы будут в папке `dist/`

---

## ⚙️ Настройка

### Конфигурация (`src/config.js`)

```javascript
export const CONFIG = {
  // URL вашего Google Apps Script
  API_URL: 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec',
  
  // Пароль для входа
  PASSWORD: '5555',
  
  // Цвет подсветки Унистрой
  UNISTROY_COLOR: '#22c55e',
};
```

### Настройка Google Apps Script

Если нужно создать новый скрипт:

1. Откройте Google-таблицу
2. **Расширения → Apps Script**
3. Вставьте код:

```javascript
function doGet(e) {
  try {
    var sheet = null;
    var sheetName = e.parameter.sheet;
    
    if (sheetName) {
      sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName(sheetName);
    } else {
      sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    }
    
    if (!sheet) {
      return ContentService
        .createTextOutput(JSON.stringify({ error: 'Лист не найден: ' + sheetName }))
        .setMimeType(ContentService.MimeType.JSON);
    }
    
    var data = sheet.getDataRange().getValues();
    var sheetNames = SpreadsheetApp.getActiveSpreadsheet().getSheets().map(function(s) {
      return s.getName();
    });
    
    var result = {
      sheetName: sheet.getName(),
      sheets: sheetNames,
      data: data
    };
    
    return ContentService
      .createTextOutput(JSON.stringify(result))
      .setMimeType(ContentService.MimeType.JSON);
      
  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({ error: error.toString() }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}
```

4. **Развернуть → Новое развёртывание**
5. Тип: **Веб-приложение**
6. Доступ: **Все, у кого есть ссылка** (или **Только я**)
7. Скопируйте URL и вставьте в `config.js`

---

## 📁 Структура проекта

```
market-share-react/
├── public/
│   └── favicon.svg
├── src/
│   ├── components/
│   │   ├── Chart.jsx
│   │   ├── Dashboard.jsx
│   │   ├── Dashboard.module.css
│   │   ├── DataTable.jsx
│   │   ├── DataTable.module.css
│   │   ├── LoginScreen.jsx
│   │   ├── LoginScreen.module.css
│   │   └── index.js
│   ├── styles/
│   │   └── index.css
│   ├── utils/
│   │   └── parser.js
│   ├── App.jsx
│   ├── config.js
│   └── main.jsx
├── .gitignore
├── index.html
├── package.json
├── README.md
└── vite.config.js
```

---

## 📋 Структура таблицы

### Листы

Каждый месяц — отдельный лист. Список листов загружается автоматически.

### Данные на листе

| Строка | Содержимое |
|--------|------------|
| 1 | Названия городов (каждые 7 колонок) |
| 2 | "Застройщики" |
| 3 | Заголовки |
| 4+ | Данные застройщиков |
| ... | ... |
| N | "Проекты" |
| N+1 | Заголовки |
| N+2+ | Данные проектов |

### Колонки для каждого города (7 штук)

1. Название
2. Количество (шт.)
3. Доля в шт. (%)
4. Площадь (м²)
5. Доля в м² (%)
6. Сумма (руб.)
7. Доля в руб. (%)

---

## 🎨 Возможности

- ✅ Приватная таблица (не нужно публиковать)
- ✅ Автоматическая загрузка списка листов
- ✅ Динамическое определение городов
- ✅ Застройщики / Проекты
- ✅ 3 метрики: шт., м², руб.
- ✅ Подсветка Унистрой
- ✅ Интерактивные графики
- ✅ Адаптивный дизайн

---

## 🔧 Устранение проблем

### CORS ошибки

Google Apps Script должен быть развёрнут с доступом **"Все, у кого есть ссылка"**.

### Данные не загружаются

1. Проверьте URL в `config.js`
2. Убедитесь, что скрипт развёрнут
3. Откройте URL скрипта в браузере — должен вернуться JSON

### Ошибка "Лист не найден"

Проверьте названия листов в таблице.

---

## 📝 Лицензия

Для внутреннего использования Унистрой.
